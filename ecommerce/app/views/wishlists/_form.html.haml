= form_for @wishlist do |f|
  - if @wishlist.errors.any?
    #error_explanation
      %h2= "#{pluralize(@wishlist.errors.count, "error")} prohibited this wishlist from being saved:"
      %ul
        - @wishlist.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
